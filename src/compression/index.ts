export { compress } from "@/compression/compress";
export { PRESETS, DEFAULT_PRESET, LIMITS, SUPPORTED_FORMATS } from "@/compression/config";
export type {
  CompressOptions,
  CompressResult,
  CompressionPresetId,
  CompressionStats,
  SupportedFormat,
} from "@/compression/types";
